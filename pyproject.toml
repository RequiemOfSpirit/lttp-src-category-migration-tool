[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "lttp-category-migration-tool"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv==1.2.2",
    "speedruncompy==1.0.4",
]

[tool.setuptools.packages.find]
where = ["src"]
